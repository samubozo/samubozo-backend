apiVersion: v1
kind: Service
metadata:
  name: {{ include "vacation-service.fullname" . }}
  labels:
    {{- include "vacation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "vacation-service.selectorLabels" . | nindent 4 }}
