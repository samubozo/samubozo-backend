spring:
  datasource:
    url: jdbc:mysql://localhost:3306/samubozo?useSSL=false&serverTimezone=UTC
    username: spring
    password: gukshin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true