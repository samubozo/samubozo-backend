apiVersion: v1
kind: Service
metadata:
  name: approval-service
spec:
  selector:
    app: approval-service
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
