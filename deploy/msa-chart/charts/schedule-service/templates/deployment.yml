apiVersion: apps/v1
kind: Deployment
metadata:
  name: schedule-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: schedule-service
  template:
    metadata:
      labels:
        app: schedule-service
    spec:
      containers:
        - name: schedule-service
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.port }}
