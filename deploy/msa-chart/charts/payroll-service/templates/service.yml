apiVersion: v1
kind: Service
metadata:
  name: payroll-service
spec:
  selector:
    app: payroll-service
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
