apiVersion: v1
kind: Service
metadata:
  name: certificate-service
spec:
  selector:
    app: certificate-service
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
