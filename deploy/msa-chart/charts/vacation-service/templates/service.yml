apiVersion: v1
kind: Service
metadata:
  name: vacation-service
spec:
  selector:
    app: vacation-service
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
